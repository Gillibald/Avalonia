<Project Sdk="Microsoft.NET.Sdk">

  <PropertyGroup>
    <OutputType>WinExe</OutputType>
    <TargetFramework>$(AvsCurrentTargetFramework)</TargetFramework>
    <TargetLatestRuntimePatch>true</TargetLatestRuntimePatch>
    <ApplicationManifest>app.manifest</ApplicationManifest>
    <IncludeAvaloniaGenerators>true</IncludeAvaloniaGenerators>

    <!-- Native AOT publish, opt-in so the flag stays scoped to this project (a global
         PublishAot fails the netstandard tooling projects in the closure):
         dotnet publish -c Release -r win-x64 /p:TextLabAot=true -->
    <PublishAot Condition="'$(TextLabAot)' == 'true'">true</PublishAot>
  </PropertyGroup>

  <ItemGroup>
    <ProjectReference Include="..\..\src\Avalonia.Fonts.Inter\Avalonia.Fonts.Inter.csproj" />
    <ProjectReference Include="..\..\src\Avalonia.Themes.Fluent\Avalonia.Themes.Fluent.csproj" />
  </ItemGroup>

  <Import Project="..\..\build\SampleApp.props" />
  <Import Project="..\..\build\ReferenceCoreLibraries.props" />
  <Import Project="..\..\build\BuildTargets.targets" />
  <Import Project="..\..\build\SourceGenerators.props" />

</Project>
